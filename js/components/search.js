export class Search {
  static filterDestination() {
    const search = document.getElementById("places-search");
    const destination = document.querySelectorAll(".offer-city-list__item");
    const destinationArr = Array.from(destination);
  }
}
